// This component will allow logged in user to navigate in dashboard and if not logged in then user will be redirected to login page
export default function Private() {
  return <div>Private</div>;
}
